{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "cppbuild",
      "label": "make file",
      "command": "make",
      "args": ["c2v0"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "dependsOn": ["make file: clean"],
      "dependsOrder": "sequence"
    },
    {
      "type": "cppbuild",
      "label": "make file: clean",
      "command": "make",
      "args": ["clean"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build"
    }
  ]
}
